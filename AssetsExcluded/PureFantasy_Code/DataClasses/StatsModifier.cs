using UnityEngine;
using System.Collections;

public class StatsModifier : MonoBehaviour {

    //bool _active;
    //bool _timed; // if it expires
    //time _timeItWentActive; //time it was added
    //time _lifeTime; //time it will last
    //private time _expirationTime;  //calculate resulting expiration time and rather store that

    //bool _decaying //interpolate values

    //The Actor itterates over the List of buffs
    // if time is > _expirationTime it List.Remove(i) it
    // if(active) recalculate acor values

}
